# Question 1 — Python: Variables and Constants

**Format:** Multiple Choice  
**Topic:** Python Programming

---

Read the code block below and answer the questions that follow.
Note: not all lines may be valid Python.

```python
MAX_RETRIES = 5
user_name = "Ada"
1st_value = 10
PI = 3.14159
```

---

**(a)** Which of the following observations about the code above is correct?

- A. All four lines are valid Python variable declarations.
- B. `MAX_RETRIES` and `PI` follow the Python convention for constants, but `1st_value` is not a valid identifier.
- C. `user_name` is not a valid Python identifier because it contains an underscore.
- D. `1st_value` is valid because Python allows identifiers that start with digits.

**Answer:** _(write your answer here, e.g. B)_

---

**(b)** In Python, constants are:

- A. Enforced by the interpreter — attempting to reassign them raises an error.
- B. A convention only — Python does not prevent reassignment of ALL_CAPS names.
- C. Defined using the `const` keyword.
- D. Only allowed inside functions.

**Answer:** _(write your answer here)_

---

**(c)** Which variable name below follows Python's PEP 8 style guide for a regular variable?

- A. `MyUserAge`
- B. `my-user-age`
- C. `my_user_age`
- D. `MYUSERAGE`

**Answer:** _(write your answer here)_
