# Question 1 — Prove Your Environment Works

**Format:** Paste real output from your own machine
**Topic:** Python & Environment Setup

> There is no multiple choice here. We want to see what your setup looks like
> **right now**. Open your terminal (on Windows, open your WSL/Ubuntu terminal —
> not PowerShell) and run the commands below. **Copy the commands AND their real
> output** into the code block. Do not invent or paste output from anywhere
> else — we are checking *your* machine.
>
> **You do not need to install anything just for this question.** If a tool
> isn't installed yet, that is completely fine — tell us honestly and say what
> you would do next. We are finding your starting point, not failing you.

**(a)** Show your Python version. Run **one** of:

```
python --version
python3 --version
```

> If neither command works (e.g. `command not found`), write one sentence saying
> Python isn't installed yet and what you'd install to get it.

**(b)** Show your installed conda environments (run this only if you have conda/Miniconda installed):

```
conda env list
```

> If you do **not** have conda installed yet, write one sentence saying so and
> describe what you would install to get it. Honesty scores here — we are
> finding your starting point, not failing you.

**Paste your terminal session below (commands + output):**

```text
# paste here
```

---

**(c)** In one sentence: what did the Python version number tell you, and how
would you check whether a package (e.g. `pandas`) is installed in your current
environment?

**Answer:**
_(write your answer here)_
