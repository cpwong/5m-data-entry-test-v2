# Question 2 — Python: Packages, Environments, and Conda

**Format:** Multiple Choice + Short Answer  
**Topic:** Python Programming

---

## Part A — Multiple Choice

**(a)** Which command installs the `pandas` package into the currently active conda environment?

- A. `conda install pandas`
- B. `pip install pandas --global`
- C. `conda add pandas`
- D. `install pandas`

**Answer:** _(write your answer here)_

---

**(b)** A `.env` file is commonly used in Python projects to:

- A. Store encrypted passwords in compiled bytecode.
- B. Define environment variables such as API keys that should not be hard-coded in source files.
- C. Replace the `requirements.txt` file for managing package versions.
- D. Automatically activate a conda environment when the script runs.

**Answer:** _(write your answer here)_

---

**(c)** You have a file called `environment.yml`. Which command creates a conda environment from it?

- A. `conda create environment.yml`
- B. `conda env create -f environment.yml`
- C. `conda activate environment.yml`
- D. `conda load -f environment.yml`

**Answer:** _(write your answer here)_

---

## Part B — Short Answer

A teammate shares a project. The root folder contains:

```
my_project/
├── environment.yml
├── .env
├── main.py
└── utils.py
```

**(d)** ⭐ *Stretch question* — In `main.py`, write the **two import statements** needed to:
1. Load environment variables from the `.env` file using the `python-dotenv` package.
2. Import the entire `utils` module from the same project folder.

> **Hint:** The `python-dotenv` package provides a function called `load_dotenv` that you import and call to load the `.env` file. For the second import, think about how you import a local `.py` file by its filename (without the extension).

```python
# Import statement 1:

# Import statement 2:
```
