# Question 5 — Operating System Concepts

**Format:** Multiple Choice  
**Topic:** Operating Systems

---

**(a)** What is a kernel?

- A. A Python library for numerical computation.
- B. The core component of an operating system that manages hardware resources and allows software to interact with the CPU, memory, and devices.
- C. A type of virtual machine that runs inside a container.
- D. The graphical user interface (GUI) layer of an operating system.

**Answer:** _(write your answer here)_

---

**(b)** Which of the following BEST describes WSL (Windows Subsystem for Linux)?

- A. A traditional virtual machine that you set up and manage yourself, running a full, separate Linux OS with dedicated CPU and RAM, fully isolated from Windows.
- B. A feature built into Windows that runs a genuine Linux environment — including a real Linux kernel and command-line tools — inside a lightweight, tightly integrated virtual machine managed automatically by Windows, rather than a separate, full-weight VM you configure yourself.
- C. A tool that converts Windows `.exe` files so they can run on Linux.
- D. A Linux distribution that replaces Windows as the primary OS.

**Answer:** _(write your answer here)_

---

**(c)** A virtual machine (VM) differs from WSL primarily because:

- A. VMs can only run Windows; WSL can only run Linux.
- B. VMs emulate an entire separate computer with its own OS and kernel, requiring dedicated CPU and RAM allocation; WSL is more tightly integrated with the host Windows system.
- C. WSL is slower than a VM for all workloads.
- D. VMs do not require any additional software to run.

**Answer:** _(write your answer here)_

---

**(d)** In the context of terminals and shells, which statement is CORRECT?

- A. A terminal and a shell are the same program.
- B. A terminal is the interface (window) through which you interact with a shell; the shell is the program that interprets and executes your commands (e.g., bash, zsh, PowerShell).
- C. PowerShell is a Linux-only shell.
- D. Bash is the default shell on Windows 11.

**Answer:** _(write your answer here)_
